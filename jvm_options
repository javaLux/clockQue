java -jar -Dfile.encoding=UTF-8 -Djdk.gtk.version=3 --module-path ./lib --add-modules javafx.controls,javafx.fxml,javafx.graphics ClockQue-1.0.1.jar

If a Java Gdk Warning will shown like:
(java:22494): Gdk-WARNING **: 18:38:59.118: XSetErrorHandler() called with a GDK error trap pushed. Don't do that.

use JVM option -Djdk.gtk.version=3 to fix these warning (tested with OpenJDK 18.0.2)

Gtk is an open source library for GNOME bindings under Linux.